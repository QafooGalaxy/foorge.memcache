{
    "services": [
{% for item in memcache_pools %}
    {
        "name": "memcache",
        "tags": ["{{ item.name|default(item.app) }}"],
        "port": {{ item.port|default(memcache_port) }}
    }{% if not loop.last %},{% endif %}
{% endfor %}
    ]
}
